@model IEnumerable<Category>
<a class="btn btn-outline-succes mb-5" href="/dashboard/category/create">Create</a>

@if (Model.Count()==0)
{
    <p class="alert alert-warning"> Categoriya Tapilmadi</p>
}

@foreach (var category in Model)
{

    <p>
        @category.CatagoryName

    </p>
}